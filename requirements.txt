# Swisky Terminal Player — dependencies
# Core TUI / rendering
rich>=13.7.0

# Audio playback (requires system libmpv, e.g. `pacman -S mpv` on Arch)
python-mpv>=1.0.5

# Metadata extraction (ID3 / FLAC / OGG / M4A tags, embedded covers)
mutagen>=1.47.0

# Image processing pipeline for ASCII rendering
Pillow>=10.2.0
numpy>=1.26.0
scipy>=1.12.0
opencv-python-headless>=4.9.0

# Audio decoding for the FFT visualizer (raw PCM analysis, decoupled from mpv).
# soundfile/libsndfile handles local WAV/FLAC/MP3 straight off disk; it can't
# decode AAC/M4A or Opus/WebM though, which is what online/playlist tracks'
# downloaded audio almost always is — audioread (shells out to ffmpeg/
# gstreamer/etc., same as mpv itself) is the fallback decoder for that case
# (see visualizer.py's module docstring and `_decode_with_audioread`).
soundfile>=0.12.1
audioread>=3.0.1

# Filesystem watching for library hot-reload
watchdog>=4.0.0

# System stats (CPU/RAM display in settings/debug panel)
psutil>=5.9.8

# Terminal input (raw keyboard + mouse escape-sequence parsing)
readchar>=4.0.6

# Online search/playback: search hits the iTunes Search API for
# metadata (no API key/account needed — falls back to a plain YouTube
# search if iTunes has nothing), then resolves a real playable audio
# stream URL from a matching YouTube video (used by online_source.py).
# Everything here uses only the standard library (urllib) plus
# yt-dlp — no extra dependency, no credentials to configure.
# Auto-fetched lyrics (lyrics_manager.py) also only need urllib.
yt-dlp>=2024.8.6
