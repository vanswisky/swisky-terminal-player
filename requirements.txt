# Swisky Terminal Player — dependencies
# Core TUI / rendering
rich>=13.7.0

# Audio playback (requires system libmpv, e.g. `pacman -S mpv` on Arch)
python-mpv>=1.0.5

# Metadata extraction (ID3 / FLAC / OGG / M4A tags, embedded covers)
mutagen>=1.47.0

# Image processing pipeline for ASCII rendering
Pillow>=10.2.0
numpy>=1.26.0
scipy>=1.12.0
opencv-python-headless>=4.9.0

# Audio decoding for the FFT visualizer (raw PCM analysis, decoupled from mpv)
soundfile>=0.12.1
audioread>=3.0.1

# Filesystem watching for library hot-reload
watchdog>=4.0.0

# System stats (CPU/RAM display in settings/debug panel)
psutil>=5.9.8

# Terminal input (raw keyboard + mouse escape-sequence parsing)
readchar>=4.0.6
